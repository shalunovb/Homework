#include <iostream>
using namespace std;
int main(int argc, char* argv[]) {
    int x = 0;
    int y = 0;
    int z = 0;
    cin >> o;
    for (int i = 1; i <= z; i++) {
        cin >> y >> x;
        cout << 19 * x + (y + 239) * (y + 366) / 2 << endl;
    }
    return EXIT_SUCCESS;
}